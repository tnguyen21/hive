critical error; somehow we can create worktrees/agents for work when we flood the orchestrator with a bunch of issues at once. e.g. i created 18 issues and now we hav e50 agents. what?

please help me fix this

---

[Request interrupted by user for tool use]

---

import sys, json
      d = json.load(sys.stdin)
      print(f'Total working agents: {len(d[\"agents\"])}')
      from collections import Counter
      issues = Counter(a.get('current_issue_title','?') for a in d['agents'])
      for title, count in issues.most_common():
          print(f'  {count}x {title}')
      ")
  ⎿  Total working agents: 54
       3x Client photos — placeholder gallery
       3x Tech mobile UX — personal schedule view
       3x Reports and analytics dashboard
       3x Recurring appointments
       3x Service add-ons — multi-service appointments
       3x Break time and time-off blocks on schedule
       3x Appointment notes and activity history
       3x Delete and archive operations for techs and clients
       3x Simulated SMS notifications on status changes
       3x Commission tracking on daily board
       3x Payment tracking and checkout flow
       3x Conflict detection and availability checking
       3x Booking flow creates real appointment
       3x Calendar month view
       3x Edit + reschedule appointments
       3x Create appointment from dashboard
       3x Client CRUD — add client form + edit client
       3x Multi-day mock data seeding
hive is active so you cna inspect its state via hive commands as well

---

[Request interrupted by user]

---

keep going

---

}

⏺ Issues #5 and #6 are open (waiting on their dependency). The other 4 must have been picked up already. Let me check.

⏺ Bash(hive --json show w-093d2b1a6898 2>&1 | python3 -c "import json,sys; d=json.load(sys.stdin); print(f'{d[\"id\"]}: {d[\"title\"]} —
      {d[\"status\"]}' + (f' (agent:…)
  ⎿  Error: Exit code 1
     Traceback (most recent call last):
       File "<string>", line 1, in <module>
     KeyError: 'id'

     Traceback (most recent call last):
       File "<string>", line 1, in <module>
     KeyError: 'id'

⏺ Bash(hive --json show w-77d128c1c148 2>&1 | python3 -c "import json,sys; d=json.load(sys.stdin); print(f'{d[\"id\"]}: {d[\"title\"]} —
      {d[\"status\"]}' + (f' (agent:…)
  ⎿  Error: Sibling tool call errored

⏺ Bash(hive --json show w-e44ac8a3e099 2>&1 | python3 -c "import json,sys; d=json.load(sys.stdin); print(f'{d[\"id\"]}: {d[\"title\"]} —
      {d[\"status\"]}' + (f' (agent:…)
  ⎿  Error: Sibling tool call errored

⏺ Bash(hive --json show w-e7d1fd63a453 2>&1 | python3 -c "import json,sys; d=json.load(sys.stdin); print(f'{d[\"id\"]}: {d[\"title\"]} —
      {d[\"status\"]}' + (f' (agent:…)
  ⎿  Error: Sibling tool call errored

can we check our prompt. queen seems ot confuse id with issue_id or whatever when creating issues

---

[Request interrupted by user]

---

maybe not creating just whaever this error is.