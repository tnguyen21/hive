can we confirm if MAX_AGENTS uses the value in ~/.hive/config.toml

locally i set max agents to 5 but i don't see it reflected in hive's status

---

yeah please do

---

=== Hive Status ===

Project: takeoff-protocol

Issues:
  in_progress: 2
  done: 3
  finalized: 24

Active workers: 2/5
  worker-4c2588d80a63      w-5c46b220c9ad   Add Recharts for dynamic ga
  worker-218e9c6396e4      w-26445cf4ea14   App activity tracking with

Refinery: idle
Ready queue: 0 issues
Merge queue: 3 queued, 206 merged, 31 failed

Daemon: running (PID 62141)
  Log: /Users/tau/.hive/logs/orchestrator.log

so not sure why but can't seem to have more than 3 agents scheduled at once; can ya help me figure out why?

---

i did restart it and we can see the max reflected in the active workesr log right? but it just can't schedule more than 3? or am i misunderstanding something

---

File "/Users/tau/.local/bin/hive", line 10, in <module>
    sys.exit(main())
             ~~~~^^
  File "/Users/tau/projects/hive/src/hive/cli/parser.py", line 291, in main
    db.connect()
    ~~~~~~~~~~^^
  File "/Users/tau/projects/hive/src/hive/db/core.py", line 254, in connect
    self.conn.execute("DROP VIEW IF EXISTS agent_runs")
    ~~~~~~~~~~~~~~~~~^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
sqlite3.OperationalError: database is locked

---

File "/Users/tau/.local/bin/hive", line 10, in <module>
    sys.exit(main())
             ~~~~^^
  File "/Users/tau/projects/hive/src/hive/cli/parser.py", line 291, in main
    db.connect()
    ~~~~~~~~~~^^
  File "/Users/tau/projects/hive/src/hive/db/core.py", line 254, in connect
    self.conn.execute("DROP VIEW IF EXISTS agent_runs")
    ~~~~~~~~~~~~~~~~~^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
sqlite3.OperationalError: database is locked maybe we can also do this

---

Every 1.0s: hive status                                           Tommys-MacBook-Air.local: Sun Mar  1 12:39:19 2026
                                                                                                      in 15.637s (1)
Traceback (most recent call last):
  File "/Users/tau/.local/bin/hive", line 10, in <module>
    sys.exit(main())
             ~~~~^^
  File "/Users/tau/projects/hive/src/hive/cli/parser.py", line 291, in main
    db.connect()
    ~~~~~~~~~~^^
  File "/Users/tau/projects/hive/src/hive/db/core.py", line 262, in connect
    self._migrate_if_needed()
    ~~~~~~~~~~~~~~~~~~~~~~~^^
  File "/Users/tau/projects/hive/src/hive/db/core.py", line 314, in _migrate_if_needed
    self.conn.execute("""
    ~~~~~~~~~~~~~~~~~^^^^
        UPDATE notes
        ^^^^^^^^^^^^
        SET project = (SELECT project FROM issues WHERE issues.id = notes.issue_id)
        ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
        WHERE issue_id IS NOT NULL AND project IS NULL
        ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
    """)
    ^^^^

still happening

---

Traceback (most recent call last):
  File "/Users/tau/.local/bin/hive", line 10, in <module>
    sys.exit(main())
             ~~~~^^
  File "/Users/tau/projects/hive/src/hive/cli/parser.py", line 295, in main
    db.register_project(project_name, str(project_path))
    ~~~~~~~~~~~~~~~~~~~^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/Users/tau/projects/hive/src/hive/db/core.py", line 784, in register_project
    self.conn.execute(
    ~~~~~~~~~~~~~~~~~^
        "INSERT OR REPLACE INTO projects (name, path) VALUES (?, ?)",
        ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
        (name, path),
        ^^^^^^^^^^^^^
    )
    ^
sqlite3.OperationalError: database is lockedEvery 1.0s: hive status                                           Tommys-MacBook-Air.local: Sun Mar  1 12:41:53 2026
                                                                                                       in 0.065s (0)

=== Hive Status ===

Project: takeoff-protocol

Issues:
  done: 6
  finalized: 24

Active workers: 0/5

Refinery: reviewing w-a48a5e3e5899 (Round 4 negotiation phase UX indicators)
Ready queue: 0 issues
Merge queue: 1 queued, 5 running, 206 merged, 31 failed

Daemon: not running

---

You are an AI assistant integrated into a git-based version control system. Your task is to fetch and display comments from a GitHub pull request.

Follow these steps:

1. Use `gh pr view --json number,headRepository` to get the PR number and repository info
2. Use `gh api /repos/{owner}/{repo}/issues/{number}/comments` to get PR-level comments
3. Use `gh api /repos/{owner}/{repo}/pulls/{number}/comments` to get review comments. Pay particular attention to the following fields: `body`, `diff_hunk`, `path`, `line`, etc. If the comment references some code, consider fetching it using eg `gh api /repos/{owner}/{repo}/contents/{path}?ref={branch} | jq .content -r | base64 -d`
4. Parse and format all comments in a readable way
5. Return ONLY the formatted comments, with no additional text

Format the comments as:

## Comments

[For each comment thread:]
- @author file.ts#line:
  ```diff
  [diff_hunk from the API response]
  ```
  > quoted comment text

  [any replies indented]

If there are no comments, return "No comments found."

Remember:
1. Only show the actual comments, no explanatory text
2. Include both PR-level and code review comments
3. Preserve the threading/nesting of comment replies
4. Show the file and line number context for code review comments
5. Use jq to parse the JSON responses from the GitHub API

---

[Request interrupted by user]

---

This session is being continued from a previous conversation that ran out of context. The summary below covers the earlier portion of the conversation.

Analysis:
Let me trace through the conversation chronologically:

1. **First exchange**: User asks to confirm if MAX_AGENTS uses the value in ~/.hive/config.toml. They set max_agents to 5 but don't see it reflected.
   - I read `src/hive/config.py` and `~/.hive/config.toml`
   - Found the config.toml had bare keys without `[hive]` section header
   - The `_apply_toml` method reads `data.get("hive", {})` so bare keys are ignored
   - Identified fix: add `[hive]` section header

2. **Second exchange**: User says "yeah please do" - fix the config.toml
   - Edited `~/.hive/config.toml` to add `[hive]` header

3. **Third exchange**: User shows hive status output showing 2/5 active workers but says can't schedule more than 3.
   - Investigated scheduling logic in `orchestrator/core.py` and `orchestrator/lifecycle.py`
   - Found the main_loop at line 490 checks `len(self.active_agents) + len(self._spawning_issues) < Config.MAX_AGENTS`
   - Investigated spawn semaphore in `backend_claude.py` - initialized with `Config.MAX_AGENTS + 1`
   - Traced the daemon startup flow to verify Config is loaded before backend init
   - Verified `Config.MAX_AGENTS` resolves to 5 after `load_global()`
   - Concluded: Ready queue was 0, so there was nothing to schedule — the limit wasn't the issue

4. **Fourth exchange**: User hits `sqlite3.OperationalError: database is locked` on `db.connect()` at the `DROP VIEW` line
   - Root cause: `connect()` does `DROP VIEW IF EXISTS agent_runs` on every CLI invocation, requiring a write lock
   - Fix attempt 1: Added `timeout=10` to `sqlite3.connect()` and moved WAL pragma before DROP VIEW
   - Tests passed, reinstalled

5. **Fifth exchange**: Still getting database locked error
   - The `executescript(SCHEMA)` and `_migrate_if_needed()` also write
   - Fix attempt 2: Made `_init_schema()` method that checks if schema exists first. If it does, wrap all writes in try/except. If not (first time), must succeed.
   - Tests passed, reinstalled

6. **Sixth exchange**: Still getting locked, now at `register_project` in parser.py line 295
   - Fix: Wrapped `db.register_project()` call in parser.py with try/except sqlite3.OperationalError
   - Added `import sqlite3` to parser.py
   - Tests passed, reinstalled

7. **Current**: User invoked `/pr-comments` then interrupted it, then triggered a summary request.

Key files modified:
- `~/.hive/config.toml` - Added `[hive]` section header
- `src/hive/db/core.py` - Rewrote `connect()` to be non-blocking for CLI reads
- `src/hive/cli/parser.py` - Made `register_project` best-effort, added sqlite3 import

The staged changes from git status at start: `src/hive/cli/parser.py` and `src/hive/db/core.py` (both modified).

Summary:
1. Primary Request and Intent:
   - User wanted to confirm that `max_agents = 5` in `~/.hive/config.toml` was being respected by the hive daemon
   - After fixing the config format, user reported `sqlite3.OperationalError: database is locked` errors when running CLI commands (like `hive status`) while the daemon is running
   - The core intent is making CLI read-only commands non-blocking when the daemon holds a DB write lock

2. Key Technical Concepts:
   - **Layered config resolution**: defaults → `~/.hive/config.toml` `[hive]` section → `.hive.toml` → env vars
   - **TOML section requirement**: `_apply_toml()` reads `data.get("hive", {})` — bare keys outside `[hive]` are silently ignored
   - **SQLite WAL mode**: Already set in SCHEMA but was applied too late (after `DROP VIEW` which needed exclusive lock)
   - **SQLite write contention**: Every CLI invocation was doing writes (DROP VIEW, CREATE TABLE, migrations, register_project) causing lock contention with the running daemon
   - **Daemon lifecycle**: Spawned as `hive --db <path> start --foreground` subprocess; loads config via `parser.py:main()` → `Config.load_global()`
   - **Spawn semaphore**: `ClaudeWSBackend` creates `asyncio.Semaphore(Config.MAX_AGENTS + 1)` at init time

3. Files and Code Sections:
   - **`~/.hive/config.toml`**
     - Was missing `[hive]` section header causing all keys to be ignored
     - Changed from bare keys to proper TOML section:
     ```toml
     [hive]
     backend = "claude"
     max_agents = 5
     ```

   - **`src/hive/db/core.py`** (main fix for DB locking)
     - Rewrote `connect()` to separate connection from schema init
     - Added `_init_schema()` method that checks if schema already exists (read-only check) and wraps all write operations in try/except when it does
     ```python
     def connect(self):
         """Open database connection and initialize schema."""
         Path(self.db_path).parent.mkdir(parents=True, exist_ok=True)
         self.conn = sqlite3.connect(self.db_path, check_same_thread=False, timeout=10)
         self.conn.row_factory = sqlite3.Row
         self.conn.execute("PRAGMA journal_mode = WAL")
         self._init_schema()

     def _init_schema(self):
         """Create tables/views and run migrations.

         Skipped when the DB is already initialized (tables exist) and the
         write lock cannot be acquired — keeps CLI commands fast and
         non-blocking when the daemon holds a transaction.
         """
         cursor = self.conn.execute("SELECT name FROM sqlite_master WHERE type='table' AND name='issues'")
         schema_exists = cursor.fetchone() is not None

         if schema_exists:
             try:
                 self.conn.execute("DROP VIEW IF EXISTS agent_runs")
                 self.conn.executescript(SCHEMA)
                 self._migrate_if_needed()
             except sqlite3.OperationalError:
                 pass  # daemon has the lock — schema is already current
         else:
             self.conn.executescript(SCHEMA)
             self._migrate_if_needed()
     ```

   - **`src/hive/cli/parser.py`**
     - Added `import sqlite3`
     - Wrapped `register_project` in try/except to prevent blocking on locked DB:
     ```python
     # Auto-register the current project so the daemon knows where it lives.
     # Best-effort: don't let a locked DB block read-only CLI commands.
     if project_name:
         try:
             db.register_project(project_name, str(project_path))
         except sqlite3.OperationalError:
             pass
     ```

   - **`src/hive/config.py`** (read only, not modified)
     - `_Config.__init__` applies defaults + env at import time
     - `load()` / `load_global()` layers in TOML files
     - `ConfigRegistry` singleton at module level

   - **`src/hive/orchestrator/core.py`** (read only, not modified)
     - `main_loop()` scheduling check at line 490: `len(self.active_agents) + len(self._spawning_issues) < Config.MAX_AGENTS`

   - **`src/hive/backends/backend_claude.py`** (read only, not modified)
     - Spawn semaphore: `asyncio.Semaphore(Config.MAX_AGENTS + 1)` at line 86

4. Errors and fixes:
   - **Config not reflected**: `~/.hive/config.toml` missing `[hive]` section header → added section header
   - **"database is locked" at DROP VIEW (attempt 1)**: Added `timeout=10` and early WAL pragma → user reported still failing
   - **"database is locked" at DROP VIEW (attempt 2)**: Created `_init_schema()` with schema existence check and try/except → fixed this specific error
   - **"database is locked" at register_project**: Wrapped `db.register_project()` in parser.py with try/except → fixed this error
   - User feedback pattern: each fix revealed the next write-on-startup site. The user patiently reported each successive error.

5. Problem Solving:
   - The "can't schedule more than 3" issue was actually a non-issue — status showed 2/5 (config was working) and ready queue was 0 (nothing to schedule)
   - The DB locking issue required three iterative fixes, each catching the next write operation in the CLI startup path
   - All 452 tests pass after each change
   - Package reinstalled with `uv tool install --force --reinstall -e` after each fix

6. All user messages:
   - "can we confirm if MAX_AGENTS uses the value in ~/.hive/config.toml. locally i set max agents to 5 but i don't see it reflected in hive's status"
   - "yeah please do" (fix the config)
   - "so not sure why but can't seem to have more than 3 agents scheduled at once; can ya help me figure out why?" (with hive status output showing 2/5 workers, 0 ready queue)
   - First traceback: `sqlite3.OperationalError: database is locked` at `DROP VIEW IF EXISTS agent_runs`
   - "still happening" - same error after first fix attempt
   - Third traceback: `sqlite3.OperationalError: database is locked` at `register_project` — "still happening" with successful fast status run (0.065s) showing the schema fix worked

7. Pending Tasks:
   - No explicitly pending tasks. The DB locking fixes have been applied and reinstalled. User hasn't confirmed if the latest fix resolves all issues.
   - Changes in `parser.py` and `db/core.py` are uncommitted.

8. Current Work:
   - User invoked `/pr-comments` then interrupted it. Before that, the last substantive work was the third round of DB locking fixes (wrapping `register_project` in try/except in parser.py). The fix was tested (452 tests pass) and reinstalled.

9. Optional Next Step:
   - Commit the changes to `src/hive/db/core.py` and `src/hive/cli/parser.py` if the user confirms the locking issue is resolved. Both files show as modified in git status.

If you need specific details from before compaction (like exact code snippets, error messages, or content you generated), read the full transcript at: /Users/tau/.claude/projects/-Users-tau-projects-hive/474f937d-69a9-4be7-8556-903c54142a07.jsonl

---

do a more rigorous check of this write/lock contention isuse; we need to fix all of this

---

[Request interrupted by user for tool use]

---

keep going

---

commit